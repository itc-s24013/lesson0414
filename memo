git mergeとrebaseの違い
・mergeの場合は新しくコミットを作成する
・rebaseの場合は新しいコミットを作成せずにコミットの属するブランチから位置を移動するイメージ

vscodeとgistをつなげる場合
・Gistの拡張機能をインストールし、左下の方から名前とgitのトークンを入力してつなげる。
・ファイルを書き換える際はgist作成のために開いたファイルではなくコピーとして作成されたファイルを書き換える。

gitのコマンド取り消し
・git resetは歴史も書き換えてしまうのでpushをしていて他の人に影響が出てしまうようなときは使わない。
・git revertはcommitを打ち消すcommitを作成するので履歴に残る。

mergeの種類
・auto merge（成功）: この場合は自動的にcommitがかかる。
・abort（コンフリクト）: mergeをなかったことにする。
・編集（コンフリクト）: 大なりや小なりを削除してから自然な形になるように編集する。

PATH
・現在使用しているシェルの設定ファイルが設定している。

標準出力
・ファイルで全て出来ているので標準出力を行うためのファイルがある。
・画面への出力はdev/stdout。
・stdoutはリンクファイルになっている。
・/dev/pts/*でどのウィンドウに出力するかを分けている。